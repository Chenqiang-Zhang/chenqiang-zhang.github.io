# Chenqiang's Blog

This repository has been reorganized into a maintainable Hexo project.

## Development

```bash
conda run -n py312 npm install
conda run -n py312 npm run server
```

## Build

```bash
conda run -n py312 npm run build
```

## Deploy

The site is configured for GitHub Pages deployment through GitHub Actions.
